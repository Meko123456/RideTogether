<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this exists so the pre-Compose window matches the theme
         instead of flashing the platform default. -->
    <style name="Theme.RideTogether" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
